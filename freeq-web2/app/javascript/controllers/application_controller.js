import { Controller } from "@hotwired/stimulus";

// Base controller for StimulusReflex — required by stimulus_reflex.initialize.
// StimulusReflex.register("application", ...) is invoked from config/stimulus_reflex.js.
export default class ApplicationController extends Controller {}