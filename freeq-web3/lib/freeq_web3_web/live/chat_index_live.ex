defmodule FreeqWeb3Web.ChatIndexLive do
  @moduledoc """
  Deprecated: `/` and `/chat` are served by `ChatLive` (`:index`) so an active
  AV call is not torn down when opening the channel directory.

  Kept as an empty module only so old references compile if any remain.
  """
end
